# Lab 01 — First Metrics

## Goal

Verify that Prometheus can scrape the monitoring server and target server.

Tasks:

1. Open Prometheus.
2. Check **Status → Targets**.
3. Query `up`.
4. Query `node_memory_MemAvailable_bytes`.
5. Open the `/metrics` endpoint on Node Exporter.
